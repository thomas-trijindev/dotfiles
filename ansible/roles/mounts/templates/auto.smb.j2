# Managed by Ansible — NAS share map
# Format: <dir>  <options>  <location>
{% for share in nas_shares %}
{{ share }}  -fstype=cifs,credentials={{ nas_credentials_file }},uid={{ primary_user_uid }},gid={{ primary_user_uid }},iocharset=utf8  ://{{ nas_host }}/{{ share }}
{% endfor %}
